type Page @model @auth(rules: [{allow: public, provider: apiKey, operations: [read]}])
{
  id: ID! @primaryKey(sortKeyFields: ["pageType"])
  title: String!
  description: String!
  url: String!
  pageType: String!
  sortOrder: Int
}